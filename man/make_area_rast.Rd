% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster.R
\name{make_area_rast}
\alias{make_area_rast}
\title{Create Area Raster}
\usage{
make_area_rast(origin, res, size, a_fact = 1e-04)
}
\arguments{
\item{origin}{Numeric vector of length 2. Origin point
(xmin, ymax) in degrees}

\item{res}{Numeric vector of length 1 or 2. Resolution
(x, y) in degrees}

\item{size}{Numeric vector of length 2. Number of columns
and rows (ncols, nrows)}

\item{a_fact}{Numeric. Area scaling factor
(default: 1e-4 for hectares)}
}
\value{
A terra raster object with "area" as the
  variable name
}
\description{
Creates a raster where each cell contains its exact surface
area based on WGS84 ellipsoid calculations.
The raster covers a rectangular area defined by origin,
resolution, and size.
}
\examples{
\dontrun{
# Create a 10x10 cell area raster at 1-degree resolution
r <- make_area_rast(
  origin = c(-120, 50), res = 1,
  size = c(10, 10)
)
}

}
